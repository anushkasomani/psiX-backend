import { IStore } from "@herodotus_dev/mmr-core";
export declare class TreeDatabase {
    protected readonly store: IStore;
    treeUuid: string;
    nodes: InStoreTable;
    readonly rootHash: InStoreTable;
    constructor(store: IStore, treeUuid?: string);
}
export declare class InStoreTable {
    protected readonly store: IStore;
    readonly key: string;
    constructor(store: IStore, key: string);
    getFullKey(suffix: string | number): string;
    get(suffix?: string | number): Promise<string>;
    getMany(suffixes: (string | number)[]): Promise<Map<string, string>>;
    set(value: string, suffix?: string | number): Promise<void>;
    setMany(entries: Record<string, string>): Promise<void>;
}
export declare class InStoreCounter {
    private readonly store;
    readonly key: string;
    constructor(store: IStore, key: string);
    get(): Promise<number>;
    set(count: number): Promise<void>;
    increment(): Promise<number>;
}
