import CoreMMR, { IHasher, IStore } from ".";
import { InStoreTable } from "./trees-database";
export declare class PrecomputationMMR extends CoreMMR {
    private readonly parentEndIdx;
    private constructor();
    static initialize(store: IStore, hasher: IHasher, parentMmrUuid: string, mmrUuid?: string): Promise<PrecomputationMMR>;
    close(): Promise<void>;
    clear(): Promise<void>;
}
export declare class PrecomputeInStoreTable extends InStoreTable {
    private readonly parentKey;
    private readonly parentEndIdx;
    constructor(store: IStore, key: string, parentKey: any, parentEndIdx: any);
    getFullKey(idx?: number): string;
}
